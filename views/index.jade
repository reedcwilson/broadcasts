extends layout

block content
  h1=title
  - if (!everyauth.loggedIn)
    #google-login
      a(href='/auth/google', style='border: 0px')
        img(style='border: 0px', src='https://www.google.com/favicon.ico')
  - else
    h1 you're logged in
  form(action="/links/create", method="post", accept-charset="utf-8")
    input(type="text", name="content")
    button(type="submit") Submit

  //-// use a loop to show all todo items at the very bottom.
  //-for link in links 
    //-p= link.uri
    //-p= link.time
  //-else
    //-p No Links
